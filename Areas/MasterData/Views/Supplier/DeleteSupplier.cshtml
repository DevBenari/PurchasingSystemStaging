@{
    ViewBag.Title = "Delete Supplier";
    Layout = "~/Views/Shared/_TopNavbarMasterData.cshtml";
}

@using PurchasingSystem.Areas.MasterData.ViewModels;
@model SupplierViewModel

<div class="card card-cyan">
    <div class="card-header">
        <h3 class="card-title" style="color:#ffffff">@ViewBag.Title</h3>
    </div>

    <form enctype="multipart/form-data" asp-controller="Supplier" asp-action="DeleteSupplier" method="post" class="form-horizontal">
        <div class="card-body">
            <h4>Yakin hapus data ini?</h4>
            <hr />
            <input hidden asp-for="SupplierId" />
            <input hidden asp-for="SupplierCode" />
            <input hidden asp-for="SupplierName" />
            <div class="post">
                <div class="user-block">                    
                    <span class="username">
                        <a href="#">@Html.DisplayFor(model => model.SupplierName)</a>
                    </span>
                    <span class="description">@Html.DisplayFor(model => model.SupplierCode)</span>
                </div>
                <!-- /.user-block -->
            </div>
        </div>

        @if (TempData["WarningMessage"] != null)
        {
            <script type="text/javascript">
                window.onload = function () {
                    swal("Failed", '@TempData["WarningMessage"]', "warning");
                };
            </script>
        }

        <div class="card-footer">
            @*<form asp-action="DeletePengguna" method="post">*@
            @*<a class="btn btn-default" asp-action="Index" asp-controller="Supplier" asp-area="MasterData">Batal</a>*@
            <div class="btn btn-default" data-href="@Url.Action("Index", "Supplier", new { area = "MasterData" })">Cancel</div>
            <button type="submit" class="btn btn-danger float-left fw-bold"><div style="color:#ffffff">Hapus</div></button>
            @*</form>*@
        </div>
    </form>
</div>
